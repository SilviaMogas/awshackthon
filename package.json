{
  "name": "health-response-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Health Response Agent - an action-oriented AI health triage and response agent (zero-dependency MVP).",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.server.json && tsc -p tsconfig.client.json && node scripts/copy-static.mjs",
    "start": "node dist/server/index.js",
    "dev": "npm run build && node dist/server/index.js",
    "test": "tsc -p tsconfig.server.json && node --test \"dist/tests/*.test.js\"",
    "typecheck": "tsc -p tsconfig.server.json --noEmit && tsc -p tsconfig.client.json --noEmit"
  },
  "engines": {
    "node": ">=18"
  }
}
