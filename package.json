{
  "name": "health-response-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Health Response Agent - an action-oriented AI health triage and response agent (zero-runtime-dependency MVP).",
  "type": "module",
  "main": "dist/server/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "rm -rf dist",
    "build": "npx tsc -p tsconfig.server.json && npx tsc -p tsconfig.client.json && node scripts/copy-static.mjs",
    "start": "node dist/server/index.js",
    "dev": "npm run build && node dist/server/index.js",
    "test": "npx tsc -p tsconfig.server.json && node --test \"dist/tests/*.test.js\"",
    "typecheck": "npx tsc -p tsconfig.server.json --noEmit && npx tsc -p tsconfig.client.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18"
  }
}
